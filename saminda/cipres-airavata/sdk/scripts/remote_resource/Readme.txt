This directory has scripts that we run on remote resources.  In the case of teragrid resources
at least, the scripts can get a bit involved so I want to keep them someplace safe and 
versioned instead of just on the remote resource.
